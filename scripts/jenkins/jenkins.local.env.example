# Copy to jenkins.local.env and fill in your API token.
# jenkins.local.env is gitignored.
#
# Create token: Jenkins -> your user -> Security -> API Token -> Add new token
# Verified username for this setup: sohansa

JENKINS_URL=http://localhost:8080
JENKINS_USER=sohansa
JENKINS_TOKEN=replace-with-your-api-token
